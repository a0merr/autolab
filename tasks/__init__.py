"""Example task plugins for autolab.

Each module here implements the :class:`autolab.Task` interface and can be
handed straight to a :class:`autolab.Lab`. They double as the targets the CLI
``autolab replay`` reconstructs by their ``tasks.module:Class`` path.
"""
